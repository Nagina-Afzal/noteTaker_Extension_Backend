# Meet Notetaker backend — environment variables
# Copy to .env for local development. On Vercel, set these in Project Settings → Environment Variables.

# Groq API key (server-side only — never shipped to the extension).
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Supabase Postgres connection string (Project Settings → Database → Connection string → URI).
DATABASE_URL=postgresql://postgres:$@C3xJgWG7*_PJk@db.<project-ref>.supabase.co:5432/postgres

# Optional: port for local server (defaults to 3000).
PORT=3000
